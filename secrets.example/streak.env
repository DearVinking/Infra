# environment: streak —— 对应原 DearVinking/Streak 仓库的 secrets

# 完整的 config.yaml 内容（多行，用 @ 指向文件）
CONFIG_YAML=@.secrets/streak-config.yaml

# Upstash Redis
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# 可选：官方抬版本号 / 换 salt 时的兜底覆盖，两者须同时设置
TAJIDUO_APPVERSION=
TAJIDUO_DS_SALT=
